[pytest]
minversion = 8.0
addopts = -ra -q --strict-markers
testpaths = tests
pythonpath = src .
asyncio_mode = auto
markers =
    live_cashbox: tests targeting physical cashier monoblock 10.0.0.241
    integration: database and service integration tests
    unit: fast isolated unit tests
